{
  "name": "@entva/express-sanitizeurl",
  "description": "ExpressJS middleware that checks request URL for potentially breaking things",
  "author": "Max Degterev <max@degterev.me>",
  "license": "MIT",
  "readmeFilename": "README.md",
  "repository": "entva/express-sanitizeurl",
  "bugs": "https://github.com/entva/express-sanitizeurl/issues",
  "version": "2.0.0",
  "keywords": [
    "express",
    "expressjs",
    "middleware",
    "query",
    "querysting",
    "sanitize"
  ],
  "main": "dist/index.js",
  "module": "esm/index.mjs",
  "types": "esm/index.d.ts",
  "exports": {
    "import": "./esm/index.mjs",
    "require": "./dist/index.js",
    "types": "./esm/index.d.ts"
  },
  "files": [
    "dist",
    "esm"
  ],
  "scripts": {
    "clean": "rm -rf ./dist ./esm",
    "prebuild": "npm run clean",
    "build:cjs": "tsc -p tsconfig-build.json --module commonjs --moduleResolution bundler --outDir dist",
    "build:esm": "tsc -p tsconfig-build.json --target es2018 --outDir esm && mv esm/index.js esm/index.mjs",
    "build": "npm run build:esm && npm run build:cjs",
    "reinstall": "rm -rf node_modules package-lock.json && npm install",
    "lint": "eslint ./",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "npm run lint && npm run typecheck && npm run test",
    "prepublishOnly": "npm run check && echo \"\\033[0;32mReady to publish\\033[0m\""
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "eslint": "^9.39.4",
    "eslint-config-entva-typescript-base": "^2.45.0",
    "vitest": "^4.1.5",
    "typescript": "^6.0.3"
  },
  "peerDependencies": {
    "express": "^5.x.x"
  }
}
